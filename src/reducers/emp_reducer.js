import {Emp_Data} from '../data/mock_data';

const empReducer = (state = Emp_Data, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default empReducer;